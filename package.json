{
  "name": "vechain-notarization",
  "version": "1.0.0",
  "description": "VeChain blockchain document notarization service - CodeSandbox ready",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "dev": "node app.js",
    "test": "echo 'Server health check' && curl -f http://localhost:3000/health || echo 'Server not running'",
    "setup": "echo 'Please configure environment variables as described in CODESANDBOX_SETUP.md'"
  },
  "keywords": ["vechain", "blockchain", "notarization", "codesandbox", "demo"],
  "author": "VeChain Developer",
  "license": "MIT",
  "dependencies": {
    "@supabase/supabase-js": "^2.55.0",
    "@vechain/sdk-network": "^2.0.3",
    "body-parser": "^1.20.2",
    "dotenv": "^17.2.1",
    "express": "^4.18.2",
    "helmet": "^7.1.0",
    "multer": "^1.4.5-lts.1",
    "stripe": "^14.7.0"
  },
  "engines": {
    "node": ">=18.x"
  }
}
